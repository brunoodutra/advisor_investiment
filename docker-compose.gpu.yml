services:
  inference:
    build:
      context: .
      dockerfile: backend/Dockerfile.inference.gpu
    gpus: all
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
